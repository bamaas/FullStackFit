import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'nl.basmaas.fittrack',
  appName: 'angu',
  webDir: 'www/angu',
  bundledWebRuntime: false
};

export default config;
