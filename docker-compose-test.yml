version: '3.3'
services:

  test:
    container_name: fullstackfit_test
    image: fullstackfit_test:latest
    build:
      context: ./test
      dockerfile: Dockerfile
    volumes:
      - './test:/src'
    network_mode: host
    command: -d logs testsuites