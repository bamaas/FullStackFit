version: '3.3'
services:

  test:
    container_name: fit_test
    build:
      context: ./test
      dockerfile: Dockerfile
    volumes:
      - './test:/src'
    network_mode: host
    command: -d logs testsuites